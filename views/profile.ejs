<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Admin Profile</title>
  <link rel="stylesheet" href="/css/styles.css" />
</head>
<body>
  <div class="container">
    <!-- Main Content -->
    <main class="profile-main">
      <h1 class="profile-title">Profile</h1>
      <div class="profile-info">
        <p><strong>Name:</strong> <%= admin.profile.name %></p>
        <p><strong>Email:</strong> <%= admin.profile.email %></p>
        <p><strong>Bio:</strong> <%= admin.profile.bio %></p>

        <form class="profile-form" action="/profile/update" method="POST">
          <input class="profile-input" type="text" name="name" value="<%= admin.profile.name %>" placeholder="Name" required />
          <input class="profile-input" type="email" name="email" value="<%= admin.profile.email %>" placeholder="Email" required />
          <textarea class="profile-textarea" name="bio" placeholder="Bio" required><%= admin.profile.bio %></textarea>
          <button class="profile-button" type="submit">Update Profile</button>
        </form>
      </div>
    </main>
  </div>
</body>
</html>
